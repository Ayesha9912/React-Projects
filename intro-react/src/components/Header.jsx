function Header(){
    // console.log(MyCount);
    return(
        <header>
            {/* <h1>TodoList Counter: {MyCount}</h1>
            <button onClick={HandleIncrement}>Increment</button> */}

            <h1>Todo List</h1>
            
        </header>
    )
}
export default Header;